#### Serial config ####
CONFIG_SERIAL=y
CONFIG_GPIO=y
CONFIG_UART_CONSOLE=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# Ethernet + zperf come from the shared project/apps/common/eth_zperf.conf,
# pulled in by CMakeLists.txt when APP_ETHERNET=ON (default ON for HE).
