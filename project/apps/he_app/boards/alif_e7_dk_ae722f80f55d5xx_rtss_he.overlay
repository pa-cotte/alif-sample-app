/*
 * Ethernet moved to the shared project/apps/common/eth.overlay, applied on
 * demand via the APP_ETHERNET CMake option (see CMakeLists.txt) so that a
 * single core owns the MAC. Board-specific, non-Ethernet HE overlays go here.
 */
